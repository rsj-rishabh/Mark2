class Solution {
    public boolean checkIfExist(int[] arr) {
        //Arrays.sort(arr);
        for(int i=0; i<arr.length; i++){
            int dobl = arr[i]*2;
            for(int j=0; j<arr.length; j++){
                if(i!=j){
                    if(dobl == arr[j]){
                        return true;
                    }   
                }
            }
        }
        return false;
    }
}
